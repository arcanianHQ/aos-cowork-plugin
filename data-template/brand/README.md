# brand/ — Client Intelligence Profile

The brand profile for this client — the files `build-brand-system` harvests and
drafts: `VOICE.md`, `ICP.md`, `POSITIONING.md`, `BELIEF_PROFILE.md`,
`COMPETITIVE_LANDSCAPE.md`, `CONSTRAINT_MAP.md`, `7LAYER_DIAGNOSTIC.md`,
`PROFILE_SCORECARD.md`, `REPAIR_ROADMAP.md`.

Templates for each live in `skills/build-brand-system/reference/file-templates/`.
Empty until `build-brand-system` runs; it hard-gates on a complete profile before
`content-draft` will run.
